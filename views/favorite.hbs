<div class="col-11">
  {{> alert}}
</div>

<div class="row">
{{#each user.FavoritedProducts}}
  <div class="card co-3">
    <a href="/product/{{this.id}}" class="a-custom" style="text-decoration:none;">
      <img class="card-img-top" src="{{this.image}}" alt="Card image cap">
      <div class="card-body">
        <h5 class="card-title">{{this.name}}</h5>
        <p class="card-text">${{this.price}}</p>
      </div>
      <div class="card-footer">
        <form action="/cart/{{this.id}}" method="POST" style="display: inline-block;">
        <input type="hidden" name="num" value="1">
          <input type="hidden" name="productId" value="{{this.id}}">
          <button type="submit" class="btn btn-secondary">Add To Cart</button>
        </form>
        <form action="/favorite/{{this.id}}?_method=DELETE" method="POST" style="display: inline-block;">
          <button type="submit" class="btn btn-secondary">
            <i class="fas fa-times"></i> 
          </button>
        </form>
      </div>
    </a>
  </div>
{{/each}}
</div>

<nav aria-label="Page navigation" class="nav-bottom">
  <ul class="pagination">
    <li class="page-item">
      <a class="page-link" href="?page={{prev}}&CategoryId={{CategoryId}}" aria-label="Previous">
        <span aria-hidden="true">&laquo;</span>
        <span class="sr-only">Previous</span>
      </a>
    </li>
    {{#each page}}
    <li class="page-item"><a class="page-link" href="?page={{this}}&CategoryId={{../CategoryId}}">{{this}}</a></li>
    {{/each}}
    <li class="page-item">
      <a class="page-link" href="?page={{post}}&CategoryId={{CategoryId}}" aria-label="Next">
        <span aria-hidden="true">&raquo;</span>
        <span class="sr-only">Next</span>
      </a>
    </li>
  </ul>
</nav>
	
<!-- cart modal at right side -->
{{> rightCart}}
	